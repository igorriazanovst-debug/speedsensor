// =============================================================================
// sensor_config.h — конфигурация экземпляра датчика
//
// Редактировать перед прошивкой каждого устройства:
//   SENSOR_INSTANCE — порядковый номер экземпляра (0001, 0002, ...)
//   SENSOR_NAME     — наименование датчика
//   SENSOR_PURPOSE  — назначение
//   SENSOR_SCENARIOS— список номеров сценариев через запятую
//
// Серийный номер формируется автоматически в прошивке:
//   Serial = <FICR Unique ID>-<SENSOR_INSTANCE>
// =============================================================================

#define SENSOR_INSTANCE   "0001"

#define SENSOR_NAME       "Датчик угловой скорости"
#define SENSOR_PURPOSE    "Измерение угловой скорости"
#define SENSOR_SCENARIOS  "1,2,3,4"
